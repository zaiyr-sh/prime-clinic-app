<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/ll_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    tools:context=".ui.main.chat.calling.CallingActivity">

    <com.facebook.drawee.view.SimpleDraweeView
        android:id="@+id/iv_user"
        android:layout_width="60dp"
        android:layout_height="60dp"
        app:actualImageResource="@drawable/shape_filled_dot"
        android:layout_marginTop="@dimen/size_100dp"
        app:failureImageScaleType="centerCrop"
        app:failureImage="@drawable/shape_filled_dot"
        app:actualImageScaleType="centerCrop"/>

    <TextView
        android:id="@+id/tv_username"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/size_12dp"
        android:text="@string/doctor"
        android:textColor="@color/teal"
        android:textSize="@dimen/size_26sp" />

    <ImageView
        android:id="@+id/iv_app_logo"
        android:layout_width="@dimen/size_250dp"
        android:layout_height="@dimen/size_250dp"
        android:layout_gravity="center"
        android:layout_marginTop="@dimen/size_30dp"
        android:src="@drawable/ic_prime_clinic_large" />

    <TextView
        android:id="@+id/tv_calling"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/size_30dp"
        android:text="@string/calling"
        android:textColor="@color/teal"
        android:textSize="@dimen/size_20sp" />

    <pl.droidsonroids.gif.GifImageView
        android:id="@+id/giv_cancel"
        android:layout_width="@dimen/size_130dp"
        android:layout_height="@dimen/size_130dp"
        android:src="@drawable/ic_calling" />

</LinearLayout>